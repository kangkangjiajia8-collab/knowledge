# 版本读取与回溯验证

本页用于验证知识库的版本发布链路，也是 agent 面向本仓库的最小读取说明。

- 当前版本：读取根目录 `manifest.json` 的 `version`。
- 历史版本：读取根目录 `CHANGELOG.md`，或执行 `git tag --list`。
- 领域明细：读取 `okf/<domain>/log.md`。
- 固定快照：检出目标 tag；发布 tag 与提交一一对应，不应移动或覆盖。

该说明作为第二次同日发布的知识库变更，用于验证版本后缀会从 `v2026.09.01` 递增为 `v2026.09.01.2`。
